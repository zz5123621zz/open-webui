server {
    listen 80;
    listen [::]:80;
    server_name chat.la4rain.com;

    root /var/www/acme;

    location ^~ /.well-known/acme-challenge/ {
        default_type text/plain;
        try_files $uri =404;
    }

    location / {
        return 503;
    }
}

# Allow narrow audited service administration

An administrator may modify a fixed, application-defined set of audited
service settings in addition to reading other users' conversations for
diagnosis; initially the only setting is progressive summary delivery. This
deliberately enables no-restart operational control while preserving workspace
isolation: service settings cannot expose credentials, accept arbitrary
provider configuration, or grant unrestricted system administration.

# Promise event delivery, not provider cadence

La4RainGPT targets rendering a received summary, search, or tool event within
one second under validation conditions, but promises no deadline for the first
semantic progress event because its creation is controlled by the provider.
While the provider is silent the UI shows only factual elapsed activity, which
trades the appearance of continuous thought for an honest and testable service
boundary.
